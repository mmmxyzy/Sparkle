# Sparkle 更新通道

Pegasus Cool 各产品的更新文件仓库，目录按产品划分，路径与更新服务器一一对应：

```
typene/
├── appcast.xml          Sparkle 更新源（App 内置地址 https://update.pegasuscool.com/typene/appcast.xml）
└── Typene-1.33.1.dmg    安装包（已公证装订，9269100 字节）
```

注意：appcast.xml 里的 length 与 DMG 字节数必须精确一致，改动文件后须重新生成，不可手改。
